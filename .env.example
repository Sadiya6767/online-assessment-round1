# Server Configuration
PORT=5000
NODE_ENV=production

# Security & Authentication
JWT_SECRET=nexis_super_secure_jwt_secret_key_change_me_in_production_2026

# Data Retention Policy (Hours before candidate records & resumes auto-purge)
RETENTION_HOURS=24
